# 需求规格说明书（SRS）

## 汽车零部件表面缺陷智能检测系统

| 项目 | 内容 |
|---|---|
| 文档版本 | v1.0 |
| 项目名称 | parts-defect-inspection-system |
| 架构形态 | B/S（浏览器/服务器） |
| 数据集 | NEU 东北大学表面缺陷数据库 |
| 编写日期 | 2026-09 |

---

## 1 引言

### 1.1 目的
本文档规定汽车零部件表面缺陷智能检测系统的功能需求、非功能需求、
数据需求与接口需求，作为设计、开发、测试与验收的依据。

### 1.2 范围
系统面向制造质量控制环节：质检员通过浏览器上传工件表面图片，
系统自动完成缺陷区域定位、缺陷类别判定、严重等级划分，
并将结果持久化、对历史数据做合格率与 SPC 统计分析。
系统为纯软件可运行 demo，不依赖真实产线硬件。

### 1.3 术语
- NEU：东北大学表面缺陷数据库，6 类缺陷共 1800 张；
- SPC：统计过程控制，用控制图监控工艺稳定性；
- Otsu：大津法，自动确定图像二值化阈值；
- B/S：浏览器/服务器架构。

## 2 总体描述

### 2.1 用户角色
| 角色 | 职责 | 主要操作 |
|---|---|---|
| 质检员 | 执行检测 | 上传图片、查看判定结果 |
| 质量工程师 | 质量分析 | 查看看板、SPC、历史记录 |

### 2.2 运行环境
- 服务端：Python 3.10+，Windows/Linux；
- 依赖：FastAPI、Uvicorn、OpenCV、scikit-learn、SQLite；
- 客户端：现代浏览器（Chrome/Edge），无需安装插件。

### 2.3 业务主流程
上传图片 → 后端解码 → 传统视觉定位缺陷区域 → 机器学习分类与评级
→ 结果写库 → 返回标注图与判定 → 前端刷新看板与历史。

## 3 功能需求

### 3.1 图像上传（FR-01）
- 支持 jpg/png/bmp；单文件上传；前端可预览原图；
- 空文件/无法解码时返回 400 并提示。

### 3.2 缺陷区域定位（FR-02，技术方向①）
- 灰度化、高斯去噪、Otsu 阈值分割、形态学开闭运算、轮廓提取；
- 输出每个缺陷候选区的外接矩形（x,y,w,h）与几何特征；
- 过滤面积 <20px 与 >40% 整图的无效连通块。

### 3.3 缺陷分类与评级（FR-03，技术方向②）
- 输出 6 类之一：crazing/inclusion/patches/pitted_surface/
  rolled-in_scale/scratches，或良品 normal；
- 输出置信度（0~1）；
- 严重等级：良品 / 轻微缺陷 / 严重缺陷；
- 有训练权重时用模型，无权重时规则分类兜底。

### 3.4 结果展示（FR-04）
- 展示标注图（红色缺陷框）、中英文类别、置信度、等级（绿/黄/红标签）；
- 展示面积占比、长宽比等关键特征。

### 3.5 历史记录（FR-05）
- 每次检测写入 SQLite；
- 前端表格展示最近 20 条：时间、文件名、类别、置信度、等级、面积占比；
- 接口支持 limit 参数。

### 3.6 质量统计看板（FR-06，技术方向③）
- 总检测数、合格率；
- 缺陷类型分布柱状图；
- 缺陷面积占比 SPC 单值控制图（CL/UCL/LCL = 均值±3σ）。

## 4 非功能需求

| 编号 | 类别 | 要求 |
|---|---|---|
| NFR-01 | 性能 | 单张图片端到端推理 < 200ms（CPU） |
| NFR-02 | 易用性 | 双击启动脚本即可运行，浏览器自动打开 |
| NFR-03 | 可维护性 | 算法/接口/数据库分层解耦，模块可独立替换 |
| NFR-04 | 可移植性 | 不依赖云服务与专有硬件，本地可跑 |
| NFR-05 | 可靠性 | 缺模型权重时降级为规则分类，不中断服务 |
| NFR-06 | 可测试性 | 提供 tests/test_api.py 冒烟测试 |

## 5 数据需求

### 5.1 输入数据
NEU 数据集，6 类×300 张，200×200 灰度 bmp；
预处理 resize 至 224×224，按 8:2 划分 train/val。

### 5.2 数据库表 inspection_records
| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | 自增主键 |
| timestamp | TEXT | 检测时间 |
| image_name | TEXT | 原始文件名 |
| defect_class | TEXT | 缺陷英文类别 |
| confidence | REAL | 置信度 |
| severity | TEXT | 严重等级 |
| defect_area_ratio | REAL | 缺陷面积占比 |
| bbox | TEXT(JSON) | 缺陷框列表 |
| raw_features | TEXT(JSON) | 原始特征向量 |

## 6 接口需求

| 方法 | 路径 | 入参 | 出参 |
|---|---|---|---|
| GET | / | 无 | 前端页面 HTML |
| POST | /api/detect | multipart 文件 file | 类别/置信度/等级/缺陷框/标注图(base64) |
| GET | /api/history?limit=n | 无 | 历史记录数组 |
| GET | /api/stats | 无 | 合格率/分布/SPC 聚合数据 |

## 7 验收标准
1. 双击启动脚本，浏览器自动进入系统页面；
2. 上传样例图能返回缺陷框、类别、等级并入库；
3. 看板与历史表随检测实时刷新；
4. tests/test_api.py 全部通过；
5. 三个技术方向模块均可在代码中定位并现场讲解。
