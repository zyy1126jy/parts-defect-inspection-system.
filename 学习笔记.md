# 学习笔记

> 阶段一：vibe coding 方法学习、AI 工具调研、git 原理、选题调研。

## 一、AI 编程工具（Harness）学习调研

本课程设计调研了以下主流 AI 编程助手，最终选用 **Trae + DeepSeek-V4** 作为主力组合：

| 工具 | 类型 | 特点 | 是否选用 |
|---|---|---|---|
| Trae | IDE 类 Harness | 国内可直连、上下文窗口大、支持整仓读取 | ✅ 主力 |
| Claude Code | CLI 类 Harness | 长上下文、重构能力强 | 备选 |
| Qwen Code / 通义灵码 | 国内 IDE 插件 | 中文友好、免费额度 | 备选 |
| Cursor | 商业 IDE | 多文件编辑体验好 | 未选用（收费） |

**模型选择**：DeepSeek-V4（开源可本地部署、代码能力强、中文 prompt 理解好），
辅助模型 GLM-5.3 用于长文档润色。

## 二、vibe coding 方法要点（课程所学）

1. **提示词工程**：任务拆解 + 约束说明 + 验收标准三段式。
   反例："帮我写个检测系统" → 正例："用 FastAPI 写一个 /api/detect 接口，
   接收图片文件，返回 JSON 含 defect_class/confidence/bboxes，单元测试要过。"
2. **上下文工程**：先让 AI `读代码` 再改，避免它凭空生成不兼容代码；
   项目根目录放 AGENTS.md / README 写清技术栈和目录约定。
3. **规格驱动开发**：需求文档 → AI 出计划 → 分步实现 → 每步跑测试验收。
4. **调试与人工接管**：AI 出错时先看报错，不要立刻重写；
   缩小改动范围（一次只改一个函数），用 git 回滚到上一个可跑版本。

## 三、Git 原理与实践

- **工作区 / 暂存区 / 本地仓库 / 远程仓库**四层模型：
  `git add` 把改动放进暂存区，`git commit` 把暂存区固化为本地提交，
  `git push` 把本地提交同步到 GitHub。
- **小步提交原则**：一个功能一次 commit，commit message 用
  `feat: xxx` / `fix: xxx` / `docs: xxx` 前缀，便于回溯。
- **本项目提交历史**见仓库 Insights → Commits，保留了从空仓库到
  完整 B/S demo 的每一步演进。

## 四、选题调研

- **场景**：汽车冲压件表面缺陷人工质检效率低、漏检率高，是智能制造
  质量控制环节的典型痛点。
- **数据集选型**：对比了 KolektorSDD2（ Slovenian 钢箔数据，二分类）、
  GC10-DET（10 类，但下载链接失效）、**NEU 东北大学表面缺陷数据库**
  （6 类、1800 张、公开稳定），最终选 NEU。
- **技术方向映射**：
  1. 数字图像处理（OpenCV 阈值/形态学）—— 课程第 2-3 章；
  2. 机器学习缺陷分类（随机森林/CNN）—— 课程第 4-5 章；
  3. 工业大数据与 SPC 质量分析 —— 课程第 7 章。
