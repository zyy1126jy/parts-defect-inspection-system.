# 方案设计

## 一、功能需求分析

系统面向"质检员在浏览器里上传工件照片 → 立刻得到缺陷判定"的业务闭环，
分为 5 个功能模块：

1. **图像上传模块**：前端支持 jpg/png 上传，预览原图；
2. **缺陷检测模块**：后端调用算法流水线，返回缺陷类别、置信度、缺陷框；
3. **结果展示模块**：前端叠加红色缺陷框，用颜色标签区分良品/轻微/严重；
4. **历史查询模块**：SQLite 持久化，前端表格展示最近 20 条记录；
5. **质量看板模块**：ECharts 渲染缺陷分布柱状图 + SPC 单值控制图。

非功能性需求：单张推理 < 200ms；无第三方云服务，本地可跑；
代码模块化，算法/接口/数据库解耦。

上述功能在 **B/S（浏览器 index.html）与 C/S（client/desktop_client.py 桌面端）
两种客户端中均完整提供**，两端通过同一套 HTTP 接口访问同一台服务器与同一个
SQLite 数据库，业务闭环一致、数据实时一致。

## 二、方案论证

### 2.1 检测方案

| 方案 | 优点 | 缺点 | 结论 |
|---|---|---|---|
| 纯深度学习 YOLO | 端到端、定位准 | 需大量标注、模型大、课程周期内训练不充分 | 不作为主方案 |
| 传统阈值分割 | 快、可解释 | 多类别区分能力弱 | 作为定位模块 |
| **传统视觉特征 + 随机森林** | 轻量、可解释、NEU 类别间几何差异明显 | 复杂场景略逊于深度模型 | ✅ 主方案 |

NEU 六类缺陷在"外接矩形长宽比、面积占比、灰度方差"三个特征上
区分度较好（划痕/裂纹细长，斑块面积大，麻点多小连通块），
因此用传统 CV 提取特征 + 随机森林分类在课程周期内可拿到可用精度。
`train_classifier.py` 保留了在 NEU 上训练随机森林的完整脚本，
训练后权重写入 `data/rf_model.json`，后端自动加载；
未训练时退化到规则分类，保证 demo 始终可跑。

### 2.2 架构选型

- **后端（服务器 S）**：FastAPI（异步、自动 OpenAPI 文档、类型注解清晰）；
- **数据库**：SQLite（单文件、零部署、课程 demo 足够）；
- **B/S 客户端**：原生 HTML + ECharts（不引入构建链，老师现场打开即跑）；
- **C/S 客户端**：Python 标准库 tkinter + ttk + Canvas，requests 走 HTTP，
  图片经 OpenCV 转 PNG 显示（免 Pillow），与 B/S 共用同一服务器；
- **算法**：OpenCV + scikit-learn。

## 三、系统架构

一台 FastAPI 服务器同时为 B/S 浏览器与 C/S 桌面客户端提供同一套 REST 接口：

```
   B/S 客户端：浏览器 (index.html, 原生JS+ECharts)
        │  fetch / HTTP
        │
   C/S 客户端：桌面程序 (client/desktop_client.py, tkinter)
        │  requests / HTTP
        ▼  POST /api/detect   GET /api/history   GET /api/stats   GET /api/health
FastAPI 服务器 (backend/app.py)
   ├── algorithms/traditional_cv.py   技术方向①：OpenCV 传统视觉
   ├── algorithms/classifier.py       技术方向②：随机森林/规则分类
   ├── algorithms/stats.py            技术方向③：SPC 统计
   └── database.py ──► SQLite (data/inspection.db)
```

## 四、数据集

- **名称**：NEU 表面缺陷数据库
- **官方**：http://faculty.neu.edu.cn/songkechen/zh_CN/zdylm/263270/list/index.htm
- **Kaggle**：https://www.kaggle.com/datasets/kaustubhdikshit/neu-surface-defect-database
- **规格**：6 类 × 300 张 = 1800 张，200×200 灰度 bmp
- **预处理**：`data/preprocess.py` resize 到 224×224，按 8:2 划 train/val

## 五、技术路线（开发步骤）

1. D1-D2：vibe coding 学习、选题、调研 NEU 数据集；
2. D3-D4：写选题说明、方案设计，初始化仓库；
3. D5：数据下载、预处理脚本；
4. D6-D8：FastAPI 后端 + 三个算法模块 + SQLite + 前端；
5. D9：集成调试、写设计说明书、录演示视频；
6. D10：答辩。

## 六、目录结构

```
parts-defect-inspection-system/
├── README.md
├── 学习笔记.md / 选题说明.md / 方案设计.md / 设计说明书.md / 需求规格说明书.md
├── requirements.txt
├── start.py                    # 一键启动服务器（自动开浏览器，B/S）
├── run.py                      # 只启动服务器
├── 启动检测系统.bat             # 双击启动服务器（B/S）
├── 启动桌面客户端.bat           # 双击启动 C/S 桌面客户端
├── index.html                  # B/S 前端（浏览器页面）
├── client/
│   └── desktop_client.py       # C/S 桌面客户端（tkinter）
├── backend/
│   ├── app.py                  # FastAPI 路由（含 /api/health）
│   ├── database.py             # SQLite
│   └── algorithms/
│       ├── traditional_cv.py   # 技术方向①：图像处理/机器视觉
│       ├── classifier.py       # 技术方向②：机器学习分类
│       ├── stats.py            # 技术方向③：SPC 统计
│       └── train_classifier.py
├── data/
│   ├── README.md
│   ├── preprocess.py
│   └── samples/                # 内置演示样例图
├── prompt/ai_record.json
└── tests/test_api.py
```

## 七、参考文献

[1] 张智海等. 制造智能技术基础[M]. 北京：清华大学出版社, 2022.
[2] 杨杰. 人工智能基础[M]. 北京：机械工业出版社, 2025.
[3] He X, et al. Surface defect classification of steel strip based on machine vision[J].
[4] 东北大学. NEU Surface Defect Database[DB/OL].
