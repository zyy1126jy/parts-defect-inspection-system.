# 设计说明书

## 一、背景调研与研究意义

### 1.1 行业背景
汽车制造中，冲压/热轧钢带的表面缺陷（裂纹、夹杂、斑块、麻点、氧化皮、划痕）
是影响成品率的关键因素。传统人工质检存在效率低、漏检率高、质量数据难沉淀
三大问题。机器视觉 + 机器学习替代人工质检，是《中国制造 2025》质量管控
环节的典型落地方向。

### 1.2 研究意义
- **工程意义**：验证"OpenCV 传统视觉 + 轻量分类器"在工业质检场景的可行性；
- **教学意义**：把课程里的图像处理、机器学习、SPC 统计三块知识在一个
  完整 B/S 系统里打通，避免"只跑算法 demo、不碰工程"；
- **可扩展**：后续可替换为 YOLO/ResNet 深度模型，或接入真实 MQTT/PLC
  执行分拣机构。

## 二、方案设计

详见同目录《方案设计.md》。核心：FastAPI + SQLite + 原生前端，
三个算法模块解耦。

## 三、数据集构建

### 3.1 数据来源
NEU 东北大学表面缺陷数据库，官方与 Kaggle 镜像双源（见 data/README.md）。

### 3.2 数据规模与类别
6 类 × 300 张 = 1800 张，200×200 灰度 bmp。

### 3.3 预处理
`data/preprocess.py` 完成：
1. 灰度读取；
2. resize 到 224×224（对齐主流模型输入尺寸）；
3. 按 8:2 划分 train/val；
4. 输出 `index.txt` 索引。

> 因仓库体积限制，原始图片不入库，仅保留脚本与链接说明。

## 四、三个技术方向的详细设计

### 4.1 技术方向一：数字图像处理与传统机器视觉
对应 `backend/algorithms/traditional_cv.py`。

流水线：
```
彩色图 → 灰度化 → 高斯模糊(5×5) → Otsu 自适应阈值(INV)
       → 开运算去噪点 → 闭运算补洞 → findContours 取外轮廓
       → 逐个轮廓算面积/周长/外接矩形/最小外接矩形长宽比/灰度均值方差
```

关键参数选择：
- 面积过滤 `20 < area < 0.4×H×W`：剔除椒盐噪点和整图误检；
- Otsu 自适应阈值：避免人工指定阈值在不同光照下失效。

### 4.2 技术方向二：机器学习缺陷分类
对应 `backend/algorithms/classifier.py`。

特征向量（5 维）：
| 特征 | 含义 | 区分作用 |
|---|---|---|
| area_ratio | 缺陷面积/图像面积 | 斑块/氧化皮面积大 |
| aspect_ratio | 最小外接矩形长宽比 | 划痕/裂纹细长 |
| gray_std | 缺陷区域灰度标准差 | 夹杂/麻点纹理乱 |
| th_mean | 二值化前景占比 | 整体缺陷程度 |
| n_cnt | 连通块数量 | 麻点多、划痕少 |

分类器：
- 训练模式：`train_classifier.py` 在 NEU 上训 RandomForest(n=100)，
  输出 `data/rf_model.json`（每类特征中心）；
- 推理模式：后端优先读权重做最近邻匹配；权重不存在时退化到
  基于规则的分类（长宽比 >4 → scratches；面积比 >0.15 → patches…），
  保证"老师现场打开就能跑"。

严重等级判定：
- 面积比 < 0.3% 且灰度差小 → 良品；
- 面积比 > 10% → 严重缺陷；其余 → 轻微缺陷。

### 4.3 技术方向三：工业大数据与 SPC 质量统计
对应 `backend/algorithms/stats.py`。

- **合格率**：良品数 / 总检测数；
- **缺陷分布**：按 6 类 + 良品统计计数，前端柱状图；
- **SPC 单值控制图**：对每次检测的 `defect_area_ratio` 序列计算
  CL = 均值，UCL/LCL = 均值 ± 3σ（总体标准差），前端画三条控制线
  + 数据点，可观察工艺漂移。

## 五、系统实现

### 5.1 后端
`backend/app.py` 提供 3 个路由：
- `GET /` 返回前端页面；
- `POST /api/detect` 接收 UploadFile，跑算法流水线，写 SQLite，返回
  JSON（含 base64 标注图）；
- `GET /api/history?limit=20` 返回最近记录；
- `GET /api/stats` 返回看板聚合数据。

### 5.2 数据库
SQLite 单表 `inspection_records`：
id / timestamp / image_name / defect_class / confidence /
severity / defect_area_ratio / bbox(JSON) / raw_features(JSON)。

### 5.3 前端
`index.html`：两栏图片对比（原图/结果图）+ 判定信息 +
ECharts 双图（缺陷分布、SPC）+ 历史表格。

## 六、界面设计
见 `index.html`，主色调工业蓝 (#217ad9)，严重等级用绿/黄/红三色标签。

## 七、集成调试

1. **环境**：Python 3.10+，`pip install -r requirements.txt`；
2. **启动**：`python run.py`，浏览器打开 http://127.0.0.1:8000；
3. **冒烟测试**：`python tests/test_api.py` 校验 /api/stats 和 /api/history；
4. **调试记录**：
   - 初版前端是纯静态页（点按钮换图），被老师要求"真业务闭环"后
     重写为调后端 API；
   - OpenCV 在 Windows 下中文路径读图会失败，改为 `np.frombuffer + imdecode`；
   - SQLite 首次启动表不存在，在 `get_conn()` 里每次 `CREATE IF NOT EXISTS`。

## 八、AI 使用披露

- **工具**：Trae IDE + DeepSeek-V4；
- **关键 prompt 策略**：
  1. 先给完整目录结构和技术栈，再让 AI 写单文件；
  2. 每个算法模块写完立刻 `python -c "import ..."` 验证语法；
  3. AI 把数据集写成 KolektorSDD2，人工发现后纠正为 NEU；
  4. AI 初版前端用了 jQuery，人工要求改为原生 JS 以降低依赖。
- **AI 出错案例**：FastAPI 静态文件路径写错导致 404，通过浏览器 Network
  面板定位，改为在 `/` 路由直接读 index.html。

## 九、总结与展望

### 9.1 完成情况
✅ B/S 架构跑通；✅ 3 个技术方向均有真实代码；✅ SQLite 持久化；
✅ SPC 看板；⚠️ 分类模型在无 NEU 权重时走规则分类，精度有限。

### 9.2 不足
1. 未接入真实相机/MQTT/PLC，分拣执行机构仅在文档中设计；
2. 分类模型未在完整 NEU 上训练，规则分类在复杂光照下会误检；
3. 未做缺陷分割掩膜，只画外接矩形。

### 9.3 展望
- 用 ResNet18/YOLOv8 替换传统特征分类，提升精度；
- 加入 ONNX 导出，支持边缘部署；
- 接入 MQTT 把缺陷等级推给 PLC，打通"检测-分拣"物理闭环。
